// Shared Tailwind class strings — import these instead of repeating the string in every file.

/** Input used in profile / account forms (white background). */
export const inputCls =
  'h-10 w-full rounded-lg border border-neutral-200 bg-white px-3 text-sm text-neutral-900 placeholder-neutral-400 outline-none transition-colors focus:border-neutral-400 focus:ring-2 focus:ring-neutral-200';

/** Input used in auth forms — login, signup, seller-setup (off-white background). */
export const authInputCls =
  'h-10 w-full rounded-lg border border-neutral-200 bg-neutral-50 px-3 text-sm text-neutral-900 placeholder-neutral-400 outline-none transition-all focus:border-neutral-400 focus:bg-white focus:ring-2 focus:ring-neutral-100';
