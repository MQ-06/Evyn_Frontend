/** Orders over this amount qualify for free shipping. */
export const SHIPPING_THRESHOLD = 50;

/** Flat shipping fee applied when the order is below the threshold. */
export const FLAT_SHIPPING = 5;
